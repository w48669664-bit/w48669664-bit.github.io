# Project media evidence

Updated 2026-09-24.

- `lumiframe-home.webp` and `lumiframe-create.webp`: captured from the actual deployed application at https://lumiframe-dxps.onrender.com/ (home and creation form). No generated UI or reconstructed screenshot. The previous editor screenshot is no longer used.
- `trade-dashboard.webp` and `trade-snapshots.webp`: captured from the actual `交易复盘工作台 2.0` React application at `/Users/sheffett/Desktop/天才交易员/trade-review-workbench`. The app was copied into an isolated temporary workspace for capture. Only seed/default account data were replaced with deterministic synthetic records; interface code and layout were preserved. Original user data were not loaded or changed. Public captions and project details label the example account.
- Other project media remain the verified screenshots and WorkRelay demonstration video collected for the initial portfolio.

No personal account screenshots, account datasets, API keys or working project data are published with the portfolio.

## Three-image galleries (2026-09-24)

All six entries now have three actual interface captures. Captures use consistent 3:2 browser viewports, displayed without an inner padded image mat.

- Lumiframe: live homepage, the real Transformer multi-head/feed-forward demonstration, and the creation form at the deployed URL above. Screenshots are captured at 1440 × 960.
- WorkRelay: decision workspace, read-only three-scenario comparison, and task/deliverable view from the actual local app, using a fresh isolated demo data directory (`/tmp/peilin-workrelay-gallery`). No existing workspace changes were applied.
- Colla Agent: current UI rendering its own completed, archived `Colla Agent Live Trace Studio` run (`relay-20260727153540`), review round 1, and the registered Launch Matrix web artifact. Playwright serves the recorded state and local artifact assets to the unchanged UI for capture. Provider usage is unavailable in this archival view; no live agent execution or account inspection was performed. Captions explicitly label the historical run. New captures replace the old image with oversized gray edges.
- 弦歌: built-in original practice example, chord timeline, and lyric editing view from the real application. No external song lyrics were introduced.
- Trade Review Workbench: daily snapshots, risk/strategy panels, and trading records, at 1920 × 1280; all use the same isolated synthetic sample account described above.
- FlowQuant: market overview, a different replay position, and sector analysis from the actual HTML application. These captures use the application's visible `DEMO / Sample Fallback` mode.

The profile portrait is the user-supplied local file `/Users/sheffett/Desktop/证件照.JPG`, copied unchanged to `public/portrait.jpg`.
